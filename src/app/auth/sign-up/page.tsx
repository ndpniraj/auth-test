import SignUp from "@/components/SignUp";
import { FC } from "react";

interface Props {}

const page: FC<Props> = (props) => {
  return (
    <div>
      <SignUp />
    </div>
  );
};

export default page;
